<!DOCTYPE html>

<title>Edit Age</title>

{% block body %}
    <h1>Edit Age</h1>

    {{ include('back/age/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_age_index') }}">back to list</a>

    {{ include('back/age/_delete_form.html.twig') }}
{% endblock %}
