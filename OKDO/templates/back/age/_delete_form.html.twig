<form method="post" action="{{ path('back_age_delete', {'id': age.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ age.id) }}">
    <button class="mt-5 py-2 px-4 bg-red-500 text-white font-semibold rounded-lg shadow-md hover:bg-Okdo focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75">Delete</button>
</form>
